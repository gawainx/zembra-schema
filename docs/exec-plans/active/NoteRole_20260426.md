# NoteRole_20260426

## Related Design Doc

[NoteRole_20260426](../../design-docs/NoteRole_20260426.md)

## Stage #1: Schema Contract Update

### Task #1: Add note role to SQLite DDL and migration

**Status:** Finished

**Files:** Modify `sqlite/001_initial_schema.sql`; Create `migrations/002_add_note_role.sql`

Function:

- Add `notes.role` as the immutable creation-role field.
- Backfill existing `0.1.0` data to `Human` during migration.

Implementation Notes:

- Use `TEXT NOT NULL DEFAULT 'Human' CHECK (role IN ('Human', 'Agent'))`.
- Insert `0.2.0` into `schema_migrations`.

Expected Verification Result:

- Fresh schema creation succeeds.
- Migration from `0.1.0` to `0.2.0` succeeds.
- `Human` and `Agent` are accepted; invalid role values are rejected.

### Task #2: Update JSON Schema and export version

**Status:** Finished

**Files:** Modify `json/note.schema.json`; Modify `json/zembra_note_export.schema.json`

Function:

- Expose `role` in cross-platform note objects.
- Bump export contract version to `0.2.0`.

Implementation Notes:

- Add `role` with enum `["Human", "Agent"]`.
- Add `role` to `required`.
- Change `schema_version.const` from `0.1.0` to `0.2.0`.

Expected Verification Result:

- Note JSON requires valid role.
- Export JSON requires schema version `0.2.0`.

## Stage #2: Documentation and Release Metadata

### Task #3: Update human-readable schema docs and changelog

**Status:** Finished

**Files:** Modify `note_schema.md`; Modify `CHANGELOG.md`

Function:

- Document the role field, migration behavior, and version bump.

Implementation Notes:

- Add `role` to the `notes` field table.
- Describe fixed enum and old-data default.
- Add `0.2.0` changelog entry.

Expected Verification Result:

- Documentation matches DDL and JSON Schema.

### Task #4: Run verification and tag release

**Status:** Finished

**Files:** Verify schema files; Git commit; Git tag `v0.2.0`

Function:

- Validate schema behavior and record release state.

Implementation Notes:

- Run SQLite fresh-create and migration checks.
- Run JSON Schema validation if project tooling is available; otherwise use a focused local validation script.
- Commit with a valid Conventional Commit message.
- Create Git tag `v0.2.0`.

Expected Verification Result:

- Verification commands pass.
- Plan statuses are updated to `Finished`.
- Git commit and `v0.2.0` tag are created.

Verification Record:

- `sqlite3 /tmp/zembra_fresh_2.db ".read sqlite/001_initial_schema.sql"` with default `Human` and explicit `Agent` insert checks passed.
- `sqlite3 /tmp/zembra_migrate_2.db ".read migrations/001_initial_schema.sql" ".read migrations/002_add_note_role.sql"` migrated legacy note to `Human` and recorded `0.2.0`.
- SQLite rejected invalid role value `agent` with the expected CHECK constraint failure.
- `python3 -m json.tool` passed for `json/note.schema.json` and `json/zembra_note_export.schema.json`.
- Built-in JSON contract check confirmed note role enum, required field, and export `0.2.0` const.
