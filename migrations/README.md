# Migrations

`sqlite/` contains SQLite migrations and the current SQLite schema snapshot. `supabase/` contains the Supabase/Postgres bootstrap, Auth membership, and RLS migrations. Apply only SQL from `migrations/supabase/` to a Supabase project.
